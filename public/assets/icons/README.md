# UI icons

UI icons are sourced from Lucide Icons. Files are fetched from the official
Lucide repository and kept as standalone SVG assets. Use `currentColor` in
consuming markup where recoloring is required.
